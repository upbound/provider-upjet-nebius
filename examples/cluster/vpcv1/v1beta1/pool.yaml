apiVersion: vpcv1.nebius.upbound.io/v1beta1
kind: Pool
metadata:
  annotations:
    meta.upbound.io/example-id: vpcv1/v1beta1/pool
  labels:
    testing.upbound.io/example-name: private-with-source-pool-child
  name: private-child
spec:
  forProvider:
    cidrs:
    - cidr: 10.10.0.0/24
      maxMaskLength: 32
    name: private-ipv4-pool-child
    parentId: ${data.nebius_project_id}
    version: IPV4
    visibility: PRIVATE
    sourcePoolIdSelector:
      matchLabels:
        testing.upbound.io/example-name: private-with-source-pool-parent
---
apiVersion: vpcv1.nebius.upbound.io/v1beta1
kind: Pool
metadata:
  annotations:
    meta.upbound.io/example-id: vpcv1/v1beta1/pool
  labels:
    testing.upbound.io/example-name: private-with-source-pool-parent
  name: private-parent
spec:
  forProvider:
    cidrs:
    - cidr: 10.10.0.0/16
      maxMaskLength: 32
    name: private-ipv4-pool-parent
    parentId: ${data.nebius_project_id}
    version: IPV4
    visibility: PRIVATE
