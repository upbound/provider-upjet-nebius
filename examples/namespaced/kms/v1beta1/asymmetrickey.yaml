apiVersion: kms.nebius.m.upbound.io/v1beta1
kind: AsymmetricKey
metadata:
  annotations:
    meta.upbound.io/example-id: kms/v1beta1/asymmetrickey
  labels:
    testing.upbound.io/example-name: default
  name: default
  namespace: upbound-system
spec:
  forProvider:
    parentId: ${data.nebius_project_id}
    name: kmsv1asymmetrickey
    algorithm: ECDSA_NIST_P256_SHA_256
